<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stephenhu.artistichorizons.dao.MaterialsDao">
    <resultMap id="BaseResultMap" type="com.stephenhu.artistichorizons.pojo.Materials">
        <id property="materialId" column="material_id"/>
        <result property="materialName" column="material_name"/>
        <result property="dateTime" column="date_time"/>
        <result property="isDel" column="is_del"/>
    </resultMap>

    <insert id="insert">
        insert into t_materials(material_id,material_name,date_time)
        values(#{materialId},#{materialName},now())
    </insert>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT material_id,
               material_name,
               date_time,
               is_del
        FROM t_materials
        WHERE material_id != 0
          AND is_del = 1
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT material_id,
               material_name,
               date_time,
               is_del
        FROM t_materials
        WHERE material_id = #{materialId}
          AND is_del = 1
    </select>

    <delete id="delById">
        UPDATE t_materials
        SET is_del = 0
        WHERE material_id = #{materialId}
    </delete>

    <update id="rollbackById">
        UPDATE t_materials
        SET is_del = 1
        WHERE material_id = #{materialId}
    </update>


    <select id="findRepeat" parameterType="map" resultMap="BaseResultMap">
        SELECT material_name,
               is_del
        FROM t_materials
        WHERE material_name=#{materialName}
          AND is_del = 1;
    </select>

</mapper>


